name: finegrained_classif
channels:
        - pytorch
        - defaults
dependencies:
        - python=3.7
        - numpy
        - pytorch=0.4
        - torchvision
        - tensorflow
        - tensorboard
        - scikit-learn
        - scikit-image
        - pip:
                - tensorboardX
